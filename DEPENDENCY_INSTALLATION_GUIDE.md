# 依赖安装指南

本指南将帮助您在服务器环境中安装阿尔茨海默病MRI分类系统的所有依赖项。

## 🔧 准备工作

### 1. 确保SSH连接正常
首先，确保您已经成功连接到服务器：
```bash
ssh -p 42264 root@connect.westc.gpuhub.com
```
输入密码：`EWuuV9a82KLr`

### 2. 导航到项目目录
连接成功后，导航到项目目录：
```bash
cd /autodl-fs/data/2025年6月8日
```

### 3. 查看项目文件
确保项目文件已正确上传到服务器：
```bash
ls -la
```
您应该能看到以下关键文件：
- `requirements.txt` - 依赖清单
- `install_dependencies.sh` - 依赖安装脚本
- `main.py` - 主程序入口

## 🚀 执行依赖安装

### 1. 给安装脚本添加执行权限
```bash
chmod +x install_dependencies.sh
```

### 2. 运行安装脚本
```bash
./install_dependencies.sh
```

### 3. 脚本执行流程
安装脚本将自动执行以下步骤：

1. **检查网络连接** - 确保服务器可以访问外部网络
2. **检查Python版本** - 确保Python版本≥3.8
3. **检查pip版本** - 确保pip版本≥22.0.0，若不是则自动升级
4. **安装依赖** - 使用pip安装`requirements.txt`中的所有依赖项
5. **验证依赖安装** - 使用`pip check`验证依赖项版本兼容性
6. **显示核心依赖版本** - 列出已安装的核心依赖版本

## 💡 常见问题与解决方案

### 1. 网络连接失败
- 检查服务器网络设置
- 尝试使用代理服务器
- 检查防火墙设置

### 2. Python版本不兼容
- 升级Python到3.8+版本：
  ```bash
  apt-get update
  apt-get install python3.8 python3.8-dev python3-pip
  ```

### 3. 依赖安装超时
- 尝试增加超时时间：
  ```bash
  pip3 install --no-cache-dir --timeout=300 -r requirements.txt
  ```

### 4. 依赖版本冲突
- 使用虚拟环境隔离依赖：
  ```bash
  python3 -m venv venv
  source venv/bin/activate
  pip3 install -r requirements.txt
  ```

### 5. 权限不足
- 使用root权限执行安装：
  ```bash
  sudo pip3 install -r requirements.txt
  ```

## ✅ 验证安装结果

### 1. 检查核心依赖版本
```bash
pip3 list | grep -E "torch|numpy|pandas|scipy|scikit-learn|nibabel|transformers"
```

### 2. 验证依赖兼容性
```bash
pip3 check
```

### 3. 运行简单测试
```bash
python3 -c "import torch; import numpy as np; import nibabel; print('依赖导入成功')"
```

## 📋 依赖清单说明

`requirements.txt`包含了项目所需的所有依赖项，按功能分类：

| 类别 | 核心依赖 | 版本要求 |
|------|----------|----------|
| 深度学习框架 | torch | ≥1.12.0 |
| 医学影像处理 | nibabel | ≥4.0.0 |
| 数据处理 | numpy, pandas | ≥1.21.0, ≥1.3.0 |
| 机器学习 | scikit-learn | ≥1.0.0 |
| NLP与Transformer | transformers | ≥4.21.0 |
| 可视化 | matplotlib, seaborn | ≥3.5.0, ≥0.11.0 |

## 🎯 下一步操作

依赖安装成功后，您可以：

1. **运行主程序**：
   ```bash
   python3 main.py
   ```

2. **运行对比学习训练**：
   ```bash
   python3 adversarial_contrastive_learning.py --mode mcic-cv --epochs 30
   ```

3. **运行MCI恢复预测**：
   ```bash
   python3 mci_recovery_prediction.py
   ```

## 📞 技术支持

如果您在安装过程中遇到任何问题，请：
1. 检查错误信息，尝试根据错误提示解决
2. 查看服务器日志文件
3. 联系项目负责人获取帮助

---

**安装完成后，祝您使用愉快！** 🚀
